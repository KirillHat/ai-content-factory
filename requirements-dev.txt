-r requirements.txt

# Linting / formatting
ruff==0.6.9

# Testing
pytest==8.3.3
pytest-cov==5.0.0

# Optional: SVG -> PNG fallback for brand logos (used by app/card_generator.py)
# Requires cairo system library (brew install cairo on macOS).
# Uncomment if you need crisp logos for brands not on App Store / unavatar.
# cairosvg==2.7.1
